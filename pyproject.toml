[project]
name = "home-ops"
version = "0.0.0"
description = "dbrennand.home_ops Ansible Project"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "ansible-navigator>=26.8.0",
]

[dependency-groups]
dev = [
    "ansible-builder>=3.1.1",
    "ansible-lint>=26.8.0",
    "prek>=0.5.0",
]
