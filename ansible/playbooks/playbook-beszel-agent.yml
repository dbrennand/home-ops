---
- name: Deploy Beszel Binary Agent
  hosts: all
  roles:
    - role: dbrennand.beszel
      vars:
        beszel_public_key: "{{ lookup('community.general.onepassword', 'Beszel', field='PUBLIC_KEY', vault='Home-Ops') }}"
